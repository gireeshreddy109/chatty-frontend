interface LoginFormProps {
    onSwitch: () => void
}

const LoginForm:React.FC<LoginFormProps> = ({onSwitch}) => {
    return (
        <div>
            //
        </div>
    )
}

export default LoginForm